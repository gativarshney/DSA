//! Array Consist of both positive and negative integers. Partition the array into two subsets such that the absolute difference between their sums is minimum. Return the minimum absolute difference.
